[project]
name = "18-tools-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "httpx>=0.28.1",
    "pytest>=9.0.2",
    "streamlit>=1.55.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
